{
  "name": "ghostty-web-server",
  "version": "0.4.0",
  "description": "Local web server that exposes a shell PTY through a ghostty-web terminal in the browser",
  "type": "module",
  "bin": {
    "ghostty-web-server": "./dist/server.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "dev": "bun --hot src/server.tsx",
    "build": "bun build src/client.tsx --outdir dist --target browser --minify --define 'process.env.NODE_ENV=\"production\"' && bun build src/server.tsx --outdir dist --target bun --minify --define 'process.env.NODE_ENV=\"production\"' && bun scripts/postbuild.ts",
    "build:bin": "bun build src/client.tsx --outdir dist --target browser --minify --define 'process.env.NODE_ENV=\"production\"' && bun build --compile --minify --bytecode --define 'process.env.NODE_ENV=\"production\"' --target=${TARGET:-bun} src/server.tsx --outfile ${OUTFILE:-dist/ghostty-web-server}",
    "build:bin:linux-x64": "TARGET=bun-linux-x64 OUTFILE=dist/ghostty-web-server-linux-x64 bun run build:bin",
    "build:bin:linux-arm64": "TARGET=bun-linux-arm64 OUTFILE=dist/ghostty-web-server-linux-arm64 bun run build:bin",
    "build:bin:darwin-arm64": "TARGET=bun-darwin-arm64 OUTFILE=dist/ghostty-web-server-darwin-arm64 bun run build:bin",
    "build:bin:darwin-x64": "TARGET=bun-darwin-x64 OUTFILE=dist/ghostty-web-server-darwin-x64 bun run build:bin"
  },
  "dependencies": {
    "elysia": "^1.4.0",
    "ghostty-web": "latest",
    "preact": "^10.27.0",
    "preact-render-to-string": "^6.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "license": "MIT",
  "author": "Coder"
}
